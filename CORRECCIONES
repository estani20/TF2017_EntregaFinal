Falta bastante para que este completo, la correcion la pueden entrgar el fin de semana, tengan en cuenta de ir haciendolo antes por si surgen dudas.
	
	
	2 - para las consultas a la BD tienen que usar la clase PDO, no mysqli (leer pautas del proyecto)
		hagan un nuevo include con conexion PDO a la base, para usarla en lo que tengan que arreglar o modificar (el resto del sitio lo cambian a la nueva conexión si tienen tiempo)
	
	4 - ABM pelicula	
		- Agregar actores y directores (hacer si tienen tiempo)
		

	6 - Validaciones	
	- Validar los datos en PHP (que no tenga caracteres raros, que sea fecha valida, que se anumero, que tenga longitud valida)
	
    - $rs -- cada vez que se ejecuta una consulta, hay que verificar si esta vacio o no, no usarlo en forma directa.
	- cuando no se utiliza mas, hay que cerrar el $rs
  	- Ej. sin validar datos PHP
		$n_poster = $_POST['inputPoster'];  no se pregunta si esta seteado, ni que clase de valor puede tener.
		
		
    7 - Hacer reporte/consulta
	Realizar una reporte de peliculas agrupadas por genero, que tenga subtotal de peliculas por genero y total de peliculas
	filtro: por genero (select - todos los generos o uno en particular) y por duracion
	orden : por calificacion o por titulo 
	exportar a excel- datos sin agrupar y sin totales
	print -  vista de impresion, con diferente estilo (el mismo archivo, modificado por estilos cuando se imprime)